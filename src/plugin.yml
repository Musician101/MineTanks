name: ${project.name}
main: ${mainClass}
version: 0.1
description: PVP minigame heavily based on World of Tanks.
author: Musician101
prefix: MineTanks

commands:
    minetanks:
        aliases: [mt]
        description: Base command the plugin.